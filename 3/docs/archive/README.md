# Archive

`learning_journey_arxiv.tex` was an arXiv-style "learning journey" paper skeleton written
before the instructor released the official report template. It is superseded by
`report/main.tex`, which uses the real template (`ai-lab-report.cls`) and is the actual
deliverable. Kept only for reference.
